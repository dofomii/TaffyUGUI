# Flex Quick Start

Create a Canvas, add an empty UI object with a `RectTransform`, attach `FlexQuickStartSample`, and enter Play Mode. The script creates three visible Taffy Flex items with explicit sizes and spacing.
